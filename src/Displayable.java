import java.util.ArrayList;

/**
 * this Displayable interface defines the behavior of objects that can be managed by LayoutManager(implements method toStringList)
 */

public interface Displayable {
    ArrayList<String> toStringList();
}
